let rec init_array arr i n = if i = n then () else (arr.(i) <- i * i; init (i+1));;

let rec show_array arr i n = if i = n then () else (let v = arr.(i) in (print_int v; print_newline()); show(i+1));;

let arr = Array.new int 5 in
    init_array arr 0 5;
    show_array arr 0 n;;
